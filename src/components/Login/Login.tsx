import {loginButton} from "./Login.css"

const Login = () => {
    return (
        <div>
            <h1>CAN I GRADUATE?</h1>
            <button className={loginButton}>Sign in</button>
        </div>
    );
};

export default Login;